/*
-> Create to a variable named x whit a valor ten.
-> Using assignment operators, achieve that x sum itself
   the double of its valor.
-> Print the result
*/

#include <stdio.h>

int main()
{
    int x = 10;

    x *= 2;

    printf("The x's value is %i", x);

    return 0;

}